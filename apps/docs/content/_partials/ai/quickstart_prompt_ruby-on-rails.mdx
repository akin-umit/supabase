```text
Help me add Supabase to my Ruby on Rails project. Create a Supabase project at database.new and copy the Session Pooler connection string. Then: 1. Run `rails new blog -d=postgresql` to scaffold a new Rails project. 2. Set `DATABASE_URL` to the Supabase Session Pooler connection string in `.env`. 3. Generate a model and run migrations with `bin/rails generate model Article title:string body:text && bin/rails db:migrate`. 4. Use `bin/rails console` to create and query records. 5. Run `bin/rails server` and open http://127.0.0.1:3000.
```
